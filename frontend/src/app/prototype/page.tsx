import App from "@/prototype/MemoryMakers";

// Bản demo all-in-one (single-file) mới nhất — đầy đủ AI Builder, flipbook lật trang, giỏ hàng, COD…
// Dữ liệu chạy trong bộ nhớ trình duyệt (không gọi API backend). Dùng để xem nhanh toàn bộ UX.
export default function PrototypePage() {
  return <App />;
}
